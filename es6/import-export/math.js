const math = {
  add (a, b) {
    return a + b
  },

  subtract (a, b) {
    return b - a
  }
}

export default math

// export const add = (a, b) => a + b

// export const subtract = (a, b) => b - a


